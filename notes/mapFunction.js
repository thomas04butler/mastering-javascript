const names = ['thomas', 'andrew', 'butler'];

const upperCaseNames = names.map((name) => name.toUpperCase());
console.log(upperCaseNames);
//.map takes each element, runs a function on it, and returns a new array of the results